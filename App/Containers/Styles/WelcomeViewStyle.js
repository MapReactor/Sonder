// @flow

import { StyleSheet } from 'react-native'
import { ApplicationStyles } from '../../Themes/'

export default StyleSheet.create({
  ...ApplicationStyles.login,
  groupContainer: {
    ...ApplicationStyles.groupContainer,
  },
})
